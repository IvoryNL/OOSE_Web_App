@using Logic.Models
@{
    ViewData["Title"] = "Onderwijs";
}
@model Leeruitkomst
<div class="onderwijs-page">
    <h3>Leeruitkomst</h3>
    <div class="content">
        @Html.ValidationSummary(true)
        @Html.ValidationMessage("all")
        @using (Html.BeginForm("MaakLeeruitkomst", "Onderwijs"))
        {
            <table>
                <tr>
                    <td>@Html.LabelFor(m => m.Naam)</td>
                    <td>@Html.EditorFor(m => m.Naam)</td>
                </tr>
                <tr>
                    <td>@Html.LabelFor(m => m.Beschrijving)</td>
                    <td>@Html.TextAreaFor(m => m.Beschrijving)</td>
                </tr>
            </table>
            @Html.HiddenFor(m => m.LeerdoelId)
            <button class="btn btn-primary">Maak</button>
        }
    </div>
</div>